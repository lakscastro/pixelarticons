name: Generate Font and Publish
on:
  push:
    branches:
      - master
  workflow_dispatch:
  schedule:
    - cron: "0 0 1 * *"

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo 'Hi from GH actions'
